 for (let x = 0; x < idx + 1; x++) {
      if ((x = 0)) {
        letterStr === letter.toUpperCase();
      } else {
        letterStr += letter.toLowerCase();
      }
    }
    newArr.push(letterStr);
  });
  return newArr.join("-");